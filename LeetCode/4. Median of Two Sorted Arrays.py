class Solution:
    def findMedianSortedArrays(self, nums1: List[int], nums2: List[int]) -> float:
        pass # i can not do this yet because i have not learned how to do a binary search, i could do this problem but the run time would be O(n+m) and not O(log(n+m))